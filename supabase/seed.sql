-- Med-X — seed
-- Runs after migrations on `supabase db reset` and `supabase start`.
-- The admin user is created via deploy/seed-admin.sh (idempotent SQL with parameters).
-- This file is intentionally minimal to keep dev resets fast.

-- (catalogue data will be seeded here once Etap F is wired)
