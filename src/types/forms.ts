export type ConsultationFormData = {
  name: string;
  phone: string;
  comment?: string;
};

export type ProductOrderFormData = {
  name: string;
  phone: string;
  productId: string;
  productTitle: string;
};
