---
name: deployment
description: Server, processes, ports, Supabase stack — single source of truth for runtime
metadata:
  type: architecture
---

## Server
- Host: `188.225.86.146` (`delau.pro`)
- OS: Ubuntu 24.04, kernel 6.8, single `/dev/sda1` (77G, ~51G free as of 2026-05-19)
- Access: SSH as `root` (user has key wired)
- Docker: 29.1.3 · Docker Compose: 2.37.1

## Med-X app (live)
- Path: `/var/www/med-x/` — git repo on branch `main`
- Deploy: `git pull` → `npm ci` → `npm run build`; nginx serves `dist/` directly (no pm2)
- Vite SPA served by **nginx** from `/var/www/med-x/dist`:
  - vhost `med-x-admin` — port **3030** (`http://188.225.86.146:3030`), used by the Vite dev proxy + seed script
  - vhost `med-x` — ports **80/443** for the public domains (below)
- `/__sb__/` proxied by both vhosts to local Supabase (`127.0.0.1:54321`)

## Domains + SSL (since 2026-05-22)
- `med-ix.ru` / `www.med-ix.ru` and `мед-икс.рф` / `www.мед-икс.рф`
  (punycode `xn----gtbcokh4b.xn--p1ai`) — all A-records → `188.225.86.146`
- Let's Encrypt cert `med-ix.ru` covers all 4 hosts, auto-renews; HTTP→HTTPS 301
- nginx vhost file: `/etc/nginx/sites-available/med-x` (symlinked into `sites-enabled/`)

## Supabase stack (Phase 2 — running)
- Path: `/var/www/med-x/supabase/` — initialised via `supabase init` on 2026-05-19
- Project ID: `med-x` (so container names are `supabase_*_med-x`)
- All ports bound to **`127.0.0.1`** (loopback only — NOT publicly exposed)
- Ports in use by the stack:

| Service | Port | URL (server-local) |
|---|---|---|
| API gateway (Kong) | 54321 | `http://127.0.0.1:54321` |
| Postgres | 54322 | `postgresql://postgres:postgres@127.0.0.1:54322/postgres` |
| Postgres shadow (for diff) | 54320 | (internal use) |
| Studio (web UI) | 54323 | `http://127.0.0.1:54323` |
| Mailpit (dev mailbox) | 54324 | `http://127.0.0.1:54324` |
| Storage S3 endpoint | 54321/storage/v1/s3 | (under API gateway) |

**Open task:** expose Supabase API publicly via nginx vhost when admin CRUD is wired (Etap F). Two options under discussion:
1. New nginx vhost: `api.med-x.local` or path `/__supabase__/*` proxying to `127.0.0.1:54321`
2. Just rebind supabase to `0.0.0.0:54321` (simpler, less elegant)

## Keys (local dev — generated by supabase CLI)
Stored in server env (`/var/www/med-x/.env.local` — to be created). **These are dev-grade keys**; supabase CLI generates fresh ones on `supabase stop && supabase start`.
- Publishable key (safe in browser) — see `obsidian/changelog/2026-05-19_phase2_supabase_up.md`
- Secret key (NEVER in browser) — same

## Other services on the same server (DO NOT TOUCH)
Visible from earlier intel:
- `nginx` on 80 / 443 (multiple vhosts in `/etc/nginx/sites-enabled/`: arcadia-meb.ru, delau.pro, ecz, plgn.space, studio, zagranuk.ru)
- Postgres 5432 (different docker stack — NOT ours)
- Redis 6379
- pm2 processes: `delau-webhook`, `prerender-delau`, `med-x` (still running on 3030 with old Next.js build)
- Various docker-proxies on 54021–54034 (NOT ours, ours are 5432x range)
- nginx port 3002, node on 9000 / 3000 / 3010 — others' apps

## ufw / firewall
`ufw status` = inactive. Cloud provider may still have its own firewall — we'll find out when we expose a new port.

## Backup
`/root/backups/` — manual backup folder on host (contents not inspected; do not delete).

## Related
- [[adr-002-self-hosted-supabase]]
- [[admin-phase2-plan]] (Etap I)
- [[stack-overview]]
